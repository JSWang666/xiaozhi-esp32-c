/**
 * Legacy placeholder: WS2812 MCP tools are registered from `esp32-s3-lcd-0.85.c`
 * / `led_strip_control.c` (see comment in led_strip_control.c). No C symbols
 * here — avoids pulling C++ `CircularStrip` into `.c` translation units.
 */
#ifndef LED_STRIP_CONTROL_H
#define LED_STRIP_CONTROL_H

#endif /* LED_STRIP_CONTROL_H */
